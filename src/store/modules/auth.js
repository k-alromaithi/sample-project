const state = {
    user: null,
    posts: null,
};

const getters = {
    StateUser: state => state.user
};

const actions = {
    async Register({dispatch}, form) {
        
    },
    
    async Login({commit}, User){

    }

};

const mutations = {

}

export default{

    state,
    getters,
    actions,
    mutations
};